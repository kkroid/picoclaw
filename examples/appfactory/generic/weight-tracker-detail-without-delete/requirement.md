# 原始需求

做一个体重记录 app，无首页，只读查看历史列表和详情页，不需要删除，优先保证本地可用。

## 需求摘录

- 打开应用后直接进入历史列表，不再保留首页趋势摘要。
- 用户只能浏览历史记录和单条详情，不提供新增、编辑和删除。
- 详情页只承载查看动作，不强绑删除入口。
- 第一阶段不做登录、云同步和分享。

## 当前假设

- 默认离线单机运行。
- 数据只保存在本地。
- 当前阶段重点验证列表到详情的只读浏览闭环。