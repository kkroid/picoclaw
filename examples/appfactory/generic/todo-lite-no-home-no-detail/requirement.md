# 原始需求

做一个待办事项 app，无首页、无详情页，直接展示待办列表，通过底部抽屉新增或编辑待办，不需要删除，也不需要筛选，优先保证本地可用。

## 需求摘录

- 打开应用后直接进入待办列表，不再保留首页摘要。
- 新增和编辑都通过底部抽屉完成，不要独立详情页。
- 只保留标题、分类、状态和备注的本地录入能力。
- 第一阶段不做登录、云同步和分享。

## 当前假设

- 默认离线单机运行。
- 数据只保存在本地。
- 当前阶段只验证列表与抽屉录入闭环，不补额外导航骨架。